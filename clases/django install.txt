python -m venv env	
Activar Entorno:
	source env/Scripts/activate	env\Scripts\activate
Instalar Django:
	pip install django	
Crear Proyecto:
	python -m django startproject config .
migraciones:
python manage.py migrate
python manage.py runserver





Paso 1: Crear una "App"
Django organiza el código en "Apps". El proyecto es el contenedor general, pero la lógica vive en apps. Con el servidor detenido (Ctrl + C), escribe:


python manage.py startapp web principal

Qué pasó: Apareció una carpeta nueva llamada principal. Dentro hay archivos como views.py.


Paso 2: Registrar la App
Django es muy ordenado; si creas algo, debes avisarle al proyecto que existe.

Abre la carpeta de tu proyecto (la que tiene el mismo nombre que tu carpeta raíz).

Busca el archivo settings.py.

Busca la lista llamada INSTALLED_APPS y agrega el nombre de tu app entre comillas al principio:

Python
INSTALLED_APPS = [
    'principal',  # <--- Agregamos esto
    'django.contrib.admin',
    ...
]
Paso 3: Crear la "Vista" (El Cerebro)
La vista es una función de Python que decide qué responder cuando alguien entra a la web.

Abre la carpeta principal y luego el archivo views.py.

Borra todo y escribe esto:

Python
from django.shortcuts import render
from django.http import HttpResponse

def hola_mundo(request):
    return HttpResponse("<h1>¡Hola Clase! Este es nuestro primer Backend</h1>")
Explicación para los alumnos: request es la petición que viene del navegador (el GET que vieron en el inspector). El return es la respuesta que enviamos de vuelta.

Paso 4: Crear la "URL" (El Mapa)
Ahora hay que decirle a Django en qué dirección vive esa función.

Ve a la carpeta del proyecto y abre urls.py.

Modifícalo para que se vea así:

Python
from django.contrib import admin
from django.urls import path
from principal.views import hola_mundo # Importamos nuestra función

urlpatterns = [
    path('admin/', admin.site.urls),
    path('saludo/', hola_mundo), # <--- Si entran a /saludo/ verán el mensaje
]
Paso 5: La prueba final (Protocolo HTTP en vivo)
Pídeles que hagan lo siguiente para cerrar la clase con éxito:

Correr el servidor: python manage.py runserver.

En el navegador, ir a: [http://127.0.0.1:8000/saludo/](http://127.0.0.1:8000/saludo/).

Acción obligatoria: Abrir el Modo Inspeccionar (F12) -> pestaña Network.

Recargar la página.

Lo que deben ver y explicar:

Verán una petición que dice saludo/.

El método es GET.

El status es 200 OK.

En la pestaña "Response" del inspector, verán el código <h1> que escribieron en el archivo views.py.

Resumen para tu pizarra mañana:
App: python manage.py startapp nombre (Crea la carpeta de trabajo).

Settings: Registrar la app en INSTALLED_APPS.

Views: Escribir la función que responde (La lógica).

URLs: Asignarle una dirección a esa función.



Resumen del Flujo de Datos (Para mañana):
Cuando todo está listo, el camino que sigue la información es:

URL: "¿A dónde quiere ir el usuario?"

VIEW: "¿Qué código debo ejecutar para esa dirección?"

RESPONSE: "Envía el resultado (HTML/Texto) de vuelta al navegador".

¿Qué les diría yo a los alumnos mañana para repasar?
Pedirles que dibujen ese triángulo en su cuaderno: URL ↔ Vista ↔ Respuesta. Si entienden que esos tres puntos están conectados, ya entendieron el 90% de cómo funciona la web.